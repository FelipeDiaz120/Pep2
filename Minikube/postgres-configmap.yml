apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-config-map
  labels:
    app: postgres-config-map
data:
  POSTGRES_DB_HOST: localhost
  #base de dato Cuotas
  POSTGRES_DB_CUOTAS: cuota
  #base de dato Estudiantes
  POSTGRES_DB_ESTUDIANTES: estudiante
  POSTGRES_DB_EXAMENES: examenes